// Total Sum is Odd, and there are even number of elements. There exists a case sum(odd indices)>sum(even indices) or vice versa. Hence, Alice can always win this
class Solution {
public:
    bool stoneGame(vector<int>& piles) {
        return true;
    }
};